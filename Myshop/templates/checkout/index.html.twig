{% extends 'base.html.twig' %}

{% block body %}

<div class="checkout-page">
    <div class="container checkout-grid">

        <!-- BÊN TRÁI: SẢN PHẨM -->
        <div class="checkout-left">
            <h3>🛒 Sản phẩm trong đơn</h3>

            {% set total = 0 %}

            {% for item in cartItems %}
                {% set subtotal = item.price * item.qty %}
                {% set total = total + subtotal %}

                <div class="checkout-item">

                    <div class="checkout-img">
                        <img src="/images/products/{{ item.image }}">
                    </div>

                    <div class="checkout-info">
                        <strong>{{ item.name }}</strong>
                        <p>Số lượng: {{ item.qty }}</p>
                        <small>{{ item.price|number_format(0,'.',',') }}₫ / SP</small>
                    </div>

                    <div class="checkout-price">
                        {{ subtotal|number_format(0,'.',',') }}₫
                    </div>

                </div>
            {% endfor %}
        </div>

        <!-- BÊN PHẢI: THANH TOÁN -->
        <div class="checkout-right">

            <h3>📦 Thông tin giao hàng</h3>
            <input type="text" placeholder="Họ và tên">
            <input type="text" placeholder="Số điện thoại">
            <input type="text" placeholder="Địa chỉ giao hàng">

            <h3>💳 Thanh toán</h3>
            <label><input type="radio" checked> Thanh toán khi nhận hàng</label>
            <label><input type="radio"> Chuyển khoản ngân hàng</label>

            <h3>🎟 Mã giảm giá</h3>
            <div class="voucher-box">
                <input type="text" placeholder="Nhập mã">
                <button>Áp dụng</button>
            </div>

            <div class="summary">
                <span>Tổng cộng:</span>
                <strong>{{ total|number_format(0,'.',',') }}₫</strong>
            </div>

            <button class="buy-btn">MUA NGAY</button>
        </div>

    </div>
</div>

<style>
.checkout-page{
    background:#ecfdf5;
    padding:100px 0;
}
.checkout-grid{
    display:grid;
    grid-template-columns:2fr 1fr;
    gap:30px;
}
.checkout-left,
.checkout-right{
    background:#ffffff;
    border-radius:22px;
    padding:25px;
    box-shadow:0 15px 40px rgba(34,197,94,.25);
}
.checkout-left h3,
.checkout-right h3{
    margin-bottom:20px;
    font-weight:800;
    color:#166534;
}
.checkout-item{
    display:grid;
    grid-template-columns:90px 1fr 140px;
    gap:16px;
    align-items:center;
    padding:16px 0;
    border-bottom:1px dashed #86efac;
}
.checkout-img{
    width:90px;
    height:80px;
    border-radius:12px;
    overflow:hidden;
    border:2px solid #86efac;
}
.checkout-img img{
    width:100%;
    height:100%;
    object-fit:cover;
}
.checkout-info strong{
    color:#166534;
}
.checkout-info small{
    color:#16a34a;
    font-weight:700;
}
.checkout-price{
    font-weight:900;
    color:#16a34a;
    text-align:right;
}
.checkout-right input{
    width:100%;
    padding:10px 14px;
    margin-bottom:12px;
    border-radius:12px;
    border:2px solid #86efac;
}
.checkout-right input:focus{
    outline:none;
    border-color:#22c55e;
}
.checkout-right label{
    display:block;
    margin-bottom:8px;
    color:#14532d;
}
.voucher-box{
    display:flex;
    gap:8px;
    margin-bottom:16px;
}
.voucher-box button{
    background:#22c55e;
    color:#fff;
    border:none;
    padding:8px 14px;
    border-radius:10px;
    font-weight:700;
}
.summary{
    display:flex;
    justify-content:space-between;
    font-size:1.2rem;
    font-weight:800;
    margin:20px 0;
    color:#166534;
}
.buy-btn{
    width:100%;
    background:linear-gradient(135deg,#22c55e,#16a34a);
    border:none;
    padding:14px;
    font-size:1.1rem;
    font-weight:900;
    color:#fff;
    border-radius:999px;
    cursor:pointer;
}
.buy-btn:hover{
    opacity:.9;
}
</style>

{% endblock %}
